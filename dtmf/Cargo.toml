[package]
name = "dtmf"
version = "1.0.0"
edition = "2024"

[dependencies]
hound = "3.5.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "3", features = ["json"] }